// We know the DOM is ready because this script isn't called until the background file is triggered,
// which will only be triggered after the DOM has been fully created. This is per Google.

// Add a div at the body level with absolute position of 0,0 and width and height of 100%.
// This will ensure that we have a full-screen overlay over the page.

// Draw vertical and horizontal lines to make a grid across the page, overtop of content

// Attach actions to the line elements for clicking and for dragging